{
  "name": "aim-trainer-local",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "lan": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "start": "node server/production-server.js",
    "check:bot": "node scripts/check-bot-navigation.mjs",
    "check:collision": "node scripts/check-player-collision.mjs",
    "check:deploy": "node scripts/check-deployment.mjs",
    "check:movement": "node scripts/check-player-movement.mjs",
    "check:smoke": "node scripts/check-production-smoke.mjs",
    "check": "node --check src/main.js && node --check src/services/account-service.js && node --check server/duel-server.js && node --check server/production-server.js && node --check scripts/check-production-smoke.mjs && node --check scripts/check-player-movement.mjs && node --check scripts/check-player-collision.mjs && pnpm run check:bot && pnpm run check:collision && pnpm run check:movement && pnpm run check:deploy && vite build && pnpm run check:smoke"
  },
  "engines": {
    "node": ">=20.19.0 || >=22.12.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "dependencies": {
    "lucide": "^1.38.0",
    "three": "^0.179.1",
    "ws": "^8.21.3"
  }
}
